{
    "routes": [
        {
            "route": "/api*",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/login",
            "serve": "/.auth/login/aad"
          }
    ]
}
